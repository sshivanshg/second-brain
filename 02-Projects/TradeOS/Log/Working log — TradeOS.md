---
created: 2026-05-30
type: log
tags: tradeos, log
---

# Working log — TradeOS

> Append-only log of decisions, blockers, and shipped work. Newest at top.

## 2026-06-01

- **Deep multi-agent stock analysis + "Ask the analyst" — turned the vague one-line verdict into the whole kachaa chitta, and made the research interrogable. 143 Python tests green (+11), ruff + mypy + svelte-check 0/0; one real deep run verified live ($0.08).** Shivansh's push: "when I tap a stock I want a proper page — full analysis by our agents: fundamentals, news, findings, how the sector is affected, how it should perform; the final verdict is too vague — explain what's actually right or wrong; and let me ask a follow-up question to an agent about the whole research." The Workbench already routed from `/briefing`, but the verdict was **one tiny Haiku call** (`AnalystVerdict`: a one-liner + ≤3 bull/bear/watch *clauses*) and the only Q&A (`docs.ask`) saw **filings only**, not the assembled facts. He chose (asked up-front): the **deepest** option (multi-agent debate) and follow-ups grounded in **facts + verdict + filings + live web**.
  - **Backend — `analyst.deep_analysis` (bull · bear · sector → judge).** Three specialist agents reason over the SAME free `assemble_facts` digest **in parallel** (`ThreadPoolExecutor`, reusing the `narrate_cards` pattern), then a judge reconciles them into a structured `DeepAnalysis` (headline · thesis · `whats_right`/`whats_wrong` each citing a number · `sector_context` · `quarter_read` · descriptive `scenarios` · `what_to_watch` · confidence · `bottom_line`). New `DEEP_MODEL` env (Sonnet 4.6 default) — 4 calls, ~$0.05–0.10, degrades to facts-only without a key, persisted to `analyst_runs` (payload gains `deep` + the raw `debate`; History renders it). `--deep` CLI flag added.
  - **Backend — `analyst.ask_research` (the follow-up over the WHOLE research).** Facts digest (memoised via `cache.memo` so repeat asks are free) + the latest persisted deep summary + RAG filing chunks (`docs.search`) + **live `web_search`** (Anthropic tool, `max_uses=3`). Non-structured `messages.create` so the tool + answer coexist (mirrors `news.py`); inline `[n]` citations validated against the retrieved chunks (`docs._valid_citations`); web sources scraped from `web_search_tool_result` blocks. Distinct route `POST /api/analyst/ask` (vs RAG-only `/api/ask`); deep read at `GET /api/analyst/{sym}/deep`. Both thin, `_sanitize`d, `_analyst_guard`ed.
  - **Frontend — the Verdict tab became the Analysis tab** (`/analyst/[symbol]`). Primary header CTA is now **Run deep analysis**; the Analysis tab renders the full read — green **What's right** / red **What's wrong** columns, a **Sector & how-it-should-perform** panel with the 3 conditional scenarios, what-to-watch, bottom line, cost line — plus the collapsible **bull/bear/sector agent debate** (the raw cited points = the "kachaa chitta"). Added **Ask the analyst**: a small chat thread (answer + "used live web" badge + web-source links + cited filing excerpts). The cheap one-liner stays as a "quick read"; the Documents tab's box relabelled **Ask the filings** to disambiguate.
  - **Honesty rails (Directives #1/#2/#7/#8) baked into every new prompt.** A shared `_DESCRIPTIVE_RULES` clause forbids buy/sell/hold + price targets + invented numbers in all four deep prompts and the ask prompt (unit-tested as a string-guard). "How it should perform" is framed as **descriptive conditional scenarios** ("IF x and y, THEN the setup descriptively implies z"), never a prediction. The sector agent is told it has **no live sector-index/peer feed** → treat sector views as context, not data (the live RELIANCE run did exactly this). Live `web_search` + live news fire **only for `as_of=None`** — barred for a historical replay (look-ahead), with a test asserting `tools` is absent when `as_of` is set.
  - **Two bugs caught in live verify (not by the mocks).** (1) **Mine:** the judge's `max_tokens=1500` truncated the rich `DeepAnalysis` JSON mid-string → `messages.parse` failed → `deep` came back `None` *even with a key*, silently defeating the feature. Fixed: specialists 1600, judge **4000** (a truncated structured response fails the parse, not just shortens — the lesson). (2) **Shivansh's `.env`:** `CLAUDE_MODEL=hai` (a leftover/typo — not a model) → `ask_research` 404s, and this **also silently breaks the existing `docs.ask` + portfolio narration**, which both read `CLAUDE_MODEL`. **NOT auto-fixed (it's the secrets file) — ACTION for Shivansh: set `CLAUDE_MODEL=claude-opus-4-8` (or remove the line) in `.env`.** Deep analysis is unaffected (it uses `DEEP_MODEL`=Sonnet), which is why the live run still worked.
  - **Verified live** (real LLM, `live_news=False`/`save=False` to keep it cheap): RELIANCE.NS deep read parsed fully — headline "Revenue growth masking an earnings deterioration story", 5 cited right / 5 cited wrong points, 3 named scenarios, honest moderate-confidence caveat (single unconfirmed bribery-probe headline, no peer data), $0.0807, 5476 in / 4284 out tokens. Degraded path (no key) returns facts + `deep=None`, JSON-safe. **+11 tests (132 → 143):** prompt bright-line guard, `_agg_usage`/`_deep_cost`/`_case_text`/`_sector_text`/`_web_sources` pure helpers, mocked happy-path orchestration (4 calls, debate dumped, cost aggregated), no-key degradation, the look-ahead web gate, + 2 API smoke tests. **Open/next:** ask-the-analyst live is blocked on the `.env` fix above; the deep read is opt-in/paid (no caching of a live run); the judge synthesis isn't token-streamed (`messages.parse` returns at once). Uncommitted (no-watermark convention).

- **Frontend scope-cull — removed the two off-scope pages so the SPA only carries the sense-making north star. svelte-check 0 errors / 0 warnings across 265 files; no dangling refs.** Shivansh's push: "old shitty stuff still exists in the frontend that isn't required — keep only what we need." Audited all 9 routes against the clarified north star (the multi-angle ANALYST ENGINE for *sense-making*, not forecasting). Kept the 6 core pages (Analyst Workbench `/analyst/[symbol]` = primary · Overview · Reasoning Monitor `/live` · Newsroom `/news` · Data Coverage `/coverage` · Manage) + Briefing (his call). **Removed `/stock/[symbol]`** — the legacy per-symbol drill-in the Evidence-Layer Workbench fully superseded (not in nav; its only inbound link was one stale `href` in `/briefing`, repointed to `/analyst/`). **Removed `/eval`** — the IC-backtest table rests on the *dead* alpha framing (empirical verdict: no bankable free-data edge), so it's off the sense-making path; `eval.py` + the CLI stay, only the UI page went.
  - **Clean orphan tail.** Mapped every shared dependency first: `priceChartOption`/`attentionGauge`/`rsiGauge`/`returnsBarOption`/`stockSeries`/`api.stock`/`api.ask` are all **shared with the Workbench** → kept. Only two things were truly orphaned and removed: `api.evalSignals()` and `charts.ts::icBarOption()`. Dropped the `/eval` nav entry. (Note: the remaining `/api/stock/...` strings in `api.ts` are *backend endpoint paths* the Workbench calls, not frontend route links — left intact.)
  - **Repo map kept honest (DoD).** Updated `CLAUDE.md` `web/` repo-map + the Phase-5 status line — removed the "stock drill-in" / "eval (IC bar chart + table)" bullets and the now-false "leaves `/stock/[symbol]` untouched" claim; the Workbench is now the *sole* per-symbol screen. Updated the `[[feedback_tradeos_agentic_ui]]` memory likewise. **Concurrent:** Shivansh is mid-build on a new `/runs` ("Briefs") page + `analystRuns` API method — left untouched, not documented as done. Uncommitted (no-watermark convention).

- **The Evidence Layer — a UI to actually SEE every fetched detail (news/articles, quarterly results, ownership, technicals, RAG corpus) + the final AI verdict, not just dials. Backend 132 Python tests green (was 124, +8), ruff + mypy clean; svelte-check 0/0; prod build clean; all 6 new endpoints verified live over HTTP incl. a real bull/bear/judge verdict.** Shivansh's push: "I need a proper view to see fetched news/articles/whatever data the agents fetched + technicals + everything + the final results; UI top-notch." The diagnosis: the engine *fetches and stores far more than the UI ever showed* — the `sentiment` table holds full headlines (title/publisher/date), `events.py` tags catalysts, the analyst has a 6-quarter results trend, `doc_chunks` is a browsable corpus, and the whole **`analyst.py` bull/bear/judge engine had ZERO UI** (CLI-only). The dashboard surfaced *summaries*; the **raw evidence was invisible**. So this builds the missing evidence layer and completes the north-star ("wrap the analyst as a dashboard page"). Chosen scope: full build, new `/analyst/[symbol]` route (leave `/stock` untouched), evidence-first then verdict.
  - **Backend — 6 thin endpoints, all reusing the engine (architecture rule held: no quant logic, no per-agent DB queries in the API).** `GET /api/analyst/{sym}?verdict=false|true` (the centerpiece — `analyst.assemble_facts` returns the entire single-name fact base for FREE: deep technical, fundamental + last-6-quarter trend, raw headlines, auto-tagged catalysts, ownership, attention/confidence; `verdict=true` adds the credibility read + ONE small Haiku bull/bear/judge call ~$0.003, degrades to facts-only without a key). `GET /api/stock/{sym}/news` (raw headlines + per-headline catalyst tag + news-flow summary). `GET /api/stock/{sym}/docs` (browse the RAG corpus). `GET /api/news` (portfolio-wide newsroom). `GET /api/coverage` (data-coverage matrix across every table). New engine helpers so the API stays thin: `sentiment.load_headlines` (title/publisher/PIT), `docs.list_documents`, new `coverage.py::data_coverage`. The analyst engine signals no-data via `SystemExit` (it's a CLI too) → mapped to a clean 404 (`_analyst_guard`). Added `_sanitize` (recursive numpy/date/Pydantic → JSON) since FastAPI's encoder chokes on `np.int64`/the `AnalystVerdict` model. Newsroom + coverage **degrade to the declared universe when holdings.csv is empty** (so they're useful for exploration, not blank).
  - **Frontend — the Analyst Workbench (`/analyst/[symbol]`).** Sticky header (identity · sector · priced-through date · vol/β · in-book weight+%CTR when held · attention + read-confidence · the verdict one-liner) over **9 evidence tabs**: Overview (every dimension's factual read + attention decomposition) · Technical (price+SMA chart, RSI gauge, returns bars, full indicator table) · Fundamentals (quarterly revenue-bars+margin-line chart + the reported-quarters table + growth + guidance quotes) · **News & Catalysts** (sentiment distribution bar, per-headline polarity timeline, the tagged-catalyst log, and the *full fetched headline feed* — title/publisher/date/polarity/event) · Ownership (institutional/insider/float donut) · Risk (standalone + in-book) · Documents (the ingested RAG corpus table + "ask the call") · Reasoning (the 6 deterministic agent reads + management-credibility) · **Verdict** (bull vs bear vs watch columns + confidence + token usage). Plus a portfolio-wide **Newsroom** (`/news`, filter by symbol/sentiment/catalyst-only) and **Data Coverage** matrix (`/coverage`, the blind-spot map). New reusable components (`NewsFeed`, `QuarterTable`, `SentimentBar`, `ProvenanceBadge`); new chart builders (`quarterlyTrendOption` dual-axis, `ownershipDonutOption`, `polarityTimelineOption`). **Polish:** a ⌘K command palette (jump to any symbol's Workbench or any page) and live Reasoning-Monitor cards now link into the Workbench.
  - **Honesty rails kept (Directives #1/#2/#7/#8):** every panel carries a `ProvenanceBadge` (`as_of` · point-in-time vs `snapshot · eval-barred`); the 6 analyzers framed as **deterministic factual reads** (only the verdict synthesises); news & ownership badged as current snapshots; single-name portfolio-relative fields (weight/%CTR) shown only when the name is actually held; the verdict steelmans both sides, cites specific numbers, names its own weakest link, and never says buy/sell.
  - **Verified live** (uvicorn :8011, fresh code): `/api/analyst/INFY.NS` → tech+fund, 5 quarters, 11 headlines, 2 catalysts, attention 33.1, confidence medium; news endpoint returns all 5 fields incl. publisher + catalyst tag; `/api/news` aggregates 822 headlines across the 108-name universe; `/api/coverage` → 108 rows with presence+freshness; unknown symbol → 404; SPA deep-links (`/analyst/...`, `/news`, `/coverage`) all serve. The **verdict path** produced a genuinely sharp INFY read (cited 18.2% net margin, −19.16% vs 200-day SMA, RSI 43.9, institutional 55.1%; flagged the Dec-25 margin swoon as the weak link; medium confidence), 823 in / 498 out tokens — proving the `AnalystVerdict` → `_sanitize` → JSON path.
  - **+8 API tests (124 → 132):** `_sanitize` (numpy/date/model coercion), analyst shape + unknown-404, stock-news shape + bad-limit-422, stock-docs, newsroom limit, coverage shape. **Open/next:** the Workbench fires two engine runs per page (analyst facts + the portfolio card for in-book %CTR) — fine for a single name, cacheable later; responses are still `any` (no shared API types); ⌘K palette lazy-loads its symbol list from `/api/coverage`. **NOTE for Shivansh: the `:8000` dev server you keep running predates these routes — restart `tradeos serve` (the SPA is rebuilt) to pick up the new backend; a throwaway verify server is on :8011.** Uncommitted (no-watermark convention).

## 2026-05-31

- **Live "Reasoning Monitor" — the multi-agent run is now watchable end-to-end (SSE), not an opaque blob. 124 Python tests green (+1), ruff + mypy + svelte-check + prod build clean; live stream verified.** Shivansh's push: "the UI isn't interactive — how do I run my agentic flow? I want to see live behind the scenes what the agents thought and responded; this manages high money, think in that direction." The honest reframing: the 6 analyzers are **deterministic compute** (not LLM "thinkers"); the only LLM step is the per-card synthesis. So "what they thought/responded" = each agent's inputs → computation → factual read, streamed as it happens, with the Claude synthesis on top — presented as a **provenance/audit ledger**, which is exactly what a real-money risk desk wants (every number traceable to its inputs + `as_of`, descriptive never prescriptive).
  - **Instrumented `orchestrator.analyze()` with an optional `on_event` observer** (default `None` ⇒ byte-identical to before; pure observation, never touches a number — keeps determinism + all existing callers/tests intact). It emits a structured event at every stage: `run_start` → `context_loaded` (the run's provenance: holdings, price rows, fundamentals/guidance/sentiment/ownership counts) → per-agent `agent_start`/`agent_done` (with a compact human read + metrics + latency + risk `data_warnings`) → `ranking` → per-holding `card` (attention decomposition across the 6 dims + confidence + drivers) → `narration_start`/`narration_done` (per-symbol Claude synthesis + token/cost/latency from `trace.py`) / `narration_skipped` → `run_complete`. `narrate_cards` switched to `as_completed` so synthesis events fire as each card lands.
  - **`GET /api/stream/analyze` (Server-Sent Events).** Runs `analyze` FRESH (uncached, `snapshot=False` — a 'watch' run shouldn't persist) in a daemon worker thread with `on_event` wired to a `queue.Queue`; the generator drains it to `text/event-stream`, paced by `STREAM_PACING_MS` (140ms default) so the deterministic cascade is watchable while LLM events arrive in real time. Numpy/date-safe JSON via a small `default=`. Still thin: the API just adapts the orchestrator's events.
  - **`/live` Reasoning Monitor page (SvelteKit + `EventSource`).** Run button + horizon/LLM-synthesis controls → opens the stream and renders: (1) point-in-time **data-load provenance** chips, (2) the **6 agent tiles** lighting up pending→running(pulse)→done with each one's note/metrics/per-holding dials/warnings + latency, (3) **per-holding reads** — attention score + the 6 sub-scores as mini-bars + confidence reasons, with the **streamed Claude synthesis** appended per card (token/cost badge), and (4) a **raw live event tape** (timestamped, auto-scroll) — the literal "behind the scenes going." Added to the sidebar as the #2 nav item.
  - **Verified live** on the example book: `run_start → context_loaded → 6×(agent_start, agent_done) → ranking → 5×card → run_complete`, rich valid payloads (risk: "book vol 16.21% ann · beta 0.9 · 99% 1d VaR 2.78% · top risk INFY 43.91%"; INFY card attention 59.9, components risk 100 / macro 100 / technical 57 …). +1 test (`/api/stream/analyze` is an event-stream, always emits `run_start` + a clean `end`, DB-or-not). **Open/next:** synthesis is whole-card (not token-by-token — `messages.parse` returns structured output at once; true token streaming would need a non-parse call); no live run is cached; LLM events only appear with `ANTHROPIC_API_KEY` (degrades to "deterministic reads complete"). Uncommitted (no-watermark convention).

- **User-facing audit → shipped black/light theme + the write seam (upload quarterly results + holdings management) Shivansh asked for. 123 Python tests green (was 117), ruff + mypy + svelte-check + prod build all clean.** Audited the *user-facing layer only* (SvelteKit SPA + FastAPI read API) on Shivansh's push ("we're missing too many things — uploading quarterly results, user input, colours aren't friendly"). Full write-up: [[User-facing audit — TradeOS]]. Verdict: the dashboard was a clean, **honest read-only viewer of a CLI-driven engine — and that was the core problem.** Everything that changes state lived only in the terminal. Shivansh chose "add a thin write seam + build both," so I did.
  - **Theme (the colour ask):** single hardcoded dark theme → **true-black default + light option**, themed CSS variables keyed on `<html data-theme>`, persisted to `localStorage`, applied before first paint (inline script in `app.html`, no flash), toggled from a sidebar button. Made **ECharts theme-aware** — `charts.ts` was a *second source of truth* for colour (baked hex duplicating `app.css`); now `getPalette()` reads the live CSS variables and pages wrap `<Chart>` in `{#key $theme}` to re-render on toggle. Fixed two colour bugs: `.dial-neutral` (referenced in `format.ts`, never defined) and a fixed near-black button text colour → themed `--on-accent` so it keeps contrast when the accent darkens on white. Added a keyboard `:focus-visible` ring (a11y).
  - **Write seam (the input ask) — stayed THIN, by the architecture rule.** Added `POST /api/holdings`, `DELETE /api/holdings/{sym}`, `POST /api/docs` (multipart file upload), `POST /api/ingest`. Each just calls the **same function the CLI uses** (`config.add_holding`/`remove_holding`, `docs.add_document`, `ingest.ingest`/`ingest_symbols`) — I/O orchestration, **no quant logic in the read layer** — then `cache.clear()` so the dashboard reflects the change. Upload is validated (pdf/txt/md, ≤25 MB, basename-sanitised into `data/uploads/`). Added `python-multipart` dep. **Decision:** crossing "no writes" but NOT "no business logic" — the bright line that mattered (the engine/read-layer purity) holds; the write routes are dumb pass-throughs. No auth ⇒ stays 127.0.0.1-only (flagged, same posture as the existing `/api/ask`).
  - **Manage page (the UI).** New `/manage` route: **holdings add/remove** (qty + avg-cost + "fetch price now"), **upload quarterly result / transcript** (symbol datalist · file · quarter-end · filing date · URL → feeds "Ask the call" + the Fundamental agent), the **document-coverage table** (finally consuming the long-built-but-unused `/api/docs/status`, colour-coded MISSING/STALE/UNTAGGED/OK), and a **refresh-data** button. Extended `lib/api.ts` with the write/coverage methods + shared error parsing.
  - **+6 API tests (117 → 123):** holdings add/remove round-trip (PORTFOLIO_FILE monkeypatched to a temp file, `fetch=False` ⇒ offline & non-destructive), upload validation (bad extension / blank symbol / empty file / bad period date). Smoke-tested a fresh server live (`POST /api/docs` bad-ext → 400) — note the **stale :8000 dev server Shivansh has running predates these routes; restart `tradeos serve` + the SPA is rebuilt to pick them up.**
  - **Still reported, not yet built** (in the audit doc): `as_of`+`horizon` UI controls (zero-backend wins), responsive/mobile nav, error-retry, first-run empty states, colour-blind-safe heatmap, shared API types (all responses are still `any`), `VITE_API_BASE` absolute-default bug, "your rules" copy over-promise. Uncommitted (no-watermark convention).

- **Second desk-audit → fixed the performance + quant-honesty findings (security left to me, by choice). 117 Python tests green (was 111), ruff + mypy clean.** Ran another full Jane-Street-bar audit of the whole platform (read every source file + schema + tests + the SvelteKit client). Verdict: the quant core is genuinely at bar and unusually *honest* — the fixes below are where a number could still quietly mislead, plus the latency/cost wins. Deliberately **left the 3 security findings for me to action** (they're mine to decide): `holdings.csv` is tracked in the **public** `psychic-engine` repo (empty today, but `.gitignore` can't protect an already-tracked file → the next `tradeos add` + commit would publish my real book — `git rm --cached holdings.csv`); a **path-traversal** in the SPA fallback (`api.spa` serves `_WEB_BUILD / path` with no `is_relative_to` guard); and the API has **no auth/rate-limit** (safe at the `127.0.0.1` default, dangerous if `API_HOST=0.0.0.0` because `/api/ask` + `?narrate=true` spend Claude tokens). SQL is clean (every `IN (...)` is placeholders-only). The throughline of what I *did* fix: **an authoritative-looking number resting on a thin/approximate sample is the failure mode the project exists to prevent — so surface the sample, never hide it.**
  - **Tail risk silently rested on a truncated sample (the honesty one).** Both the EWMA covariance and the historical VaR/CVaR P&L use `dropna(how="any")` — the *intersection* of all holdings' histories — so adding one recent name silently shrinks the whole book's "2y" VaR to that name's short window, with nothing saying so (violated my own Directives #7/#8). Fix: surface `cov_obs` / `var_obs` (the common-sample length behind the cov & VaR) in the risk dict + a `data_warnings` list that fires when `var_obs < 252` / `cov_obs < 60`. The number now carries its own sample size. → [[Validate the data, not just that it runs]]
  - **Horizon VaR was a √T approximation presented without the caveat.** `var_*_pct` scale the 1-day empirical VaR by √horizon — exact only under iid, unreliable in *either* direction at long horizons for fat-tailed/clustered equity returns. Contract-safe fix: **labelled** it (`tail_scaling="sqrt-time (iid approximation)"` + method string) rather than silently swapping in a direct h-day estimate — the 1-day VaR *limit check* was already exact, so the risk gate stays honest. Direct overlapping-h-day empirical VaR deferred (it'd change a reported number ⇒ a deliberate, tested change later).
  - **Non-positive vendor prices could poison the covariance.** `np.log(adj)` / `adj.pct_change()` on a zero/negative tick → `-inf`/`inf` silently corrupts the whole cov. Now masked to NaN first (`adj.where(adj > 0)`).
  - **Multiple-testing is now *deflated*, not just flagged.** `eval` scores ~7 signals; it warned but applied no correction. Added a per-signal two-sided `p_value` (from the Newey-West t, `erfc(|t|/√2)`) + a Bonferroni floor `bonferroni_alpha = 0.05/n` + `significant_raw`/`significant_bonferroni` flags — so the headline can't promote one lucky trial out of many. (Directive #5.)
  - **Performance (the "max output" ask).** (a) **`psycopg_pool` connection pool** in `db.py` — was opening a fresh `psycopg.connect()` per query (~6 per `analyze`); pgvector now registers once per connection via the pool `configure` hook, lazily so import needs no DB. (b) **Read-layer TTL cache** (`cache.py`, `READ_CACHE_TTL=300`, deep-copy on return so `annotate_deltas` can't corrupt it) — the factual analysis/risk/eval are memoised and the **portfolio + every per-stock page share one key**, so the engine runs once per `(as_of, horizon)`. (c) **`/api/stock/{sym}` was narrating the *entire book* to serve one card** (an LLM call per holding); now narrates only that holding (`narrate_cards([card])`). (d) Ledoit-Wolf π vectorised (was an O(n²) Python loop); one reused Anthropic client. Cache + pool live in the read layer only — **the quant core stays pure/deterministic**. → [[Earn the infra — adopt a tool only when you feel the problem it solves]]
  - **+6 tests (111 → 117):** read-cache copy-isolation/TTL-0/clear, `_p_two_sided`, eval deflation integration, risk provenance fields. Repo map + this log updated; `pyproject` adds `psycopg-pool`. **Still open (the honest headline, unchanged):** the eval is rigorous but **underpowered on a 5-name cross-section** and the survivorship universe still has **no genuinely delisted names** — the machinery is right, breadth is the binding constraint on any edge conclusion *and* on earning the score weights. Next data lift remains a wider, point-in-time, delisted-inclusive universe. Uncommitted (no-watermark convention).

- **Senior-quant audit pass → fixed 7 findings (2 wrong-number defects, a PIT leak, a reproducibility gap, + 3 hardening). 110 Python tests green (was 103), ruff + mypy clean.** Ran a full desk-style audit of the whole engine (read every source file + schema + tests; verified the "green" claim — 103 passed / 0 skipped, integration tests really hit the DB). The machinery was at bar (PIT enforced at the SQL layer + tested, Newey-West HAC correct, costs/survivorship/multiple-testing all flagged). Found and fixed where numbers were silently wrong or non-reproducible. The throughline: **a confidently-wrong number that *looks* validated is the failure mode the whole project exists to prevent.**
  - **YoY inverted sign across a negative base year (the worst one — a *wrong* number a human reads).** `_pct` guarded `old == 0` but not `old < 0`, so a loss→profit **turnaround** read as `−150% → "declining"`. Wrong in two places at once: the live Fundamental dial **and** the eval's `earnings_yoy` signal. Fix: guard `old <= 0` ⇒ no-data (a growth % off a non-positive base is undefined; a *negative new over a positive base*, profit→loss, is a real decline and is kept). → [[Validate the data, not just that it runs]]
  - **The eval tested a *different* YoY than the agent ships.** Eval used `rev.shift(4)` (4 rows back); the agent uses calendar-month matching. Across yfinance's quarter gaps `shift(4)` silently mis-pairs (a missing quarter turns a YoY into a 9-month change), so a future "edge-validated → promote weights" decision would import an edge the live agent never reproduces. Fix: one shared `fundamental.calendar_yoy_pct` (year-1/same-month, gap-robust, negative-base-guarded) used by **both** the agent and the eval. **Root cause + lesson:** validate the transform you actually trade, not a convenient proxy of it.
  - **Ownership leaked present-day data into historical card replays.** `load_ownership` took no `as_of`, so `analyze --as-of <past>` injected today's institutional % into a past card's attention + confidence (eval itself was clean — it never uses ownership). Fix: PIT-gate on `snapshot_at <= as_of` (mirrors the sentiment/guidance pattern) — a snapshot taken today is correctly invisible on a replay; NULL `snapshot_at` excluded under `as_of` (honest gap). Locked with a query-level invariant test.
  - **Back-tests weren't reproducible across re-ingests (the project bar: every number traceable to its inputs + `as_of`).** yfinance retroactively re-adjusts the whole history on every split/dividend and the `prices` UPSERT overwrites it — so the same `as_of` can return different ICs a month later, with no archive of what was known at decision time. (Return signals are adjustment-*invariant*, so no look-ahead — but reproducibility was broken.) Fix: an **append-only `price_vintages` revision log** (bitemporal) — ingest logs each (symbol,date) on first sight + only when the vendor restates it, stamped with the ingest day; `risk.load_panels_asof` reconstructs the panel as-known-at a date (latest `vintage_date <= target`); `eval.evaluate(vintage_asof=…)` opts in (default off ⇒ non-breaking). **Honest caveat baked in:** past vintages can't be backfilled — the archive only covers ingests from now on. → [[Earn the infra — adopt a tool only when you feel the problem it solves]]
  - **+3 hardening.** (a) Net-of-cost LS spread was sign-wrong for a mean-reversion signal: `gross − cost` *amplified* a negative spread (made costs look like they help a losing book). Now `|gross| − cost` = the net edge of the directionally-traded book; cost always erodes, any sign. (b) FHS conditional vol now **de-meaned** to match `_ewma_cov`'s convention, so the tail path and the covariance diagonal stop silently disagreeing. (c) ingest volume is **None-safe** (`int(NaN)` would have killed a ticker mid-run).
  - **+7 tests (103 → 110):** negative-base & calendar-matched YoY (agent≡eval), eval fundamental-feature PIT + negative-base drop, ownership PIT query invariant, vintage selection (pure) + an as-known-at round-trip. Repo map + this log updated; `universe.example.csv` corrected (it listed YESBANK/PNB — both still trade, so it demoed the mechanism with zero actual delisted names). **Still open (the honest headline):** the eval is rigorous but **underpowered to the point of non-informative** on a 5-name cross-section, and the survivorship "fix" still has **no genuinely delisted names** in it — the machinery is right, the *sample* can't yet support an edge conclusion. Uncommitted (no-watermark convention).

- **Phase 5 dashboard — proper visual rebuild with Apache ECharts (charts, gauges, heatmap).** Took the functional-but-flat SPA to a real dashboard aesthetic on Shivansh's push for "proper graphs and visuals." 103 Python tests green (+2 for the new endpoint), `svelte-check` clean, prod build clean, verified live.
  - **New chart data seam:** `technical.price_series(symbol, as_of)` — point-in-time close + SMA20/50/200 (computed on full history, then tailed so the long averages populate) + volume → `GET /api/stock/{sym}/series`. The quant core stays untouched; this is a read.
  - **Charting:** added **Apache ECharts** + a `Chart.svelte` wrapper (init/resize-observer/dispose, re-renders on `$effect`) and `lib/charts.ts` with a dark palette + option builders. Visuals now: **price + SMA overlay chart** (with dataZoom), **attention & RSI gauges** (zoned), **risk-contribution-vs-weight horizontal bars**, **sector-allocation donut**, **correlation heatmap** (green→red), **1m/3m/vs-200SMA return bars**, and an **IC-by-signal bar chart** coloured by significance (|t|≥2).
  - **Design system:** rewrote `app.css` — left **sidebar layout** (brand · nav · live holdings list), KPI tiles, panel grid, refined attention-coloured cards, TradingView-ish green/red. Pages reworked: overview (tiles + 3 charts + cards), stock drill-in (price chart + 2 gauges + returns bars + all 6 dimensions + trace + RAG ask), eval (IC chart + table), briefing (tiles + flagged panels).
  - **Stayed honest in the UI:** "no data" still renders as — / "no sentiment ingested", sentiment/ownership tagged "snapshot · eval-barred", eval prints every caveat (survivorship, multiple-testing, small-universe). Decision: adopting a charting lib (ECharts) is fine — the "build-your-own" rule was about the agent/orchestration core, not chart plumbing.
  - **Open:** real holdings.csv is empty (demo runs on the example book); only INFY/TCS have sentiment+ownership ingested. Phase 5 tail unchanged (scheduler + Telegram). Uncommitted.

- **Phase 5 underway — FastAPI read API + SvelteKit dashboard + pre-market briefing, all working end-to-end.** 101 Python tests green (ruff + mypy clean); SvelteKit `svelte-check` clean; production single-origin verified. The architecture rule held: **the API and frontend are READ layers — no business logic, no DB access; they call the exact same pure engine the CLI uses.**
  - **`api.py` (FastAPI).** Thin JSON layer: `/api/portfolio` (the 6-dim cards + attention + confidence + delta + overviews), `/api/stock/{sym}` (card + opt-in LLM trace), `/api/risk`, `/api/eval`, `/api/briefing`, `/api/docs/status`, `POST /api/ask` (RAG). Every endpoint just calls orchestrator/risk/eval/docs/briefing; engine "no data" → clean 503, bad `as_of` → 400. LLM narration opt-in (`?narrate=true`) and degrades to empty without a key, like the CLI. `tradeos serve` / `tradeos-api`. CORS open to :5173; in prod it auto-mounts the built SPA at `/` with a deep-link fallback (verified `/stock/INFY.NS` → 200 HTML).
  - **`web/` (SvelteKit + Svelte 5 + TS).** A clean SPA over the API: portfolio overview (attention-coloured cards, dials, Δ-since-last), per-stock drill-in (all six dimensions + the LLM reasoning trace + an "ask the call" RAG box), the honest signal-eval table (IC / NW-t / net-of-cost spread + every caveat printed), and the briefing page. Built-your-own (no UI kit), dark quant theme. `npm run dev` (:5173) against `tradeos serve` (:8000); `npm run build` → `web/build` served by FastAPI for a single-origin prod. **Stack choice was Shivansh's** (FastAPI + SvelteKit over Streamlit/HTMX) — the higher-effort, higher-showcase option.
  - **`briefing.py` — the roadmap's "tells you what to look at this morning".** A **pure, unit-tested alert-rule engine** (top-risk-contributor ≥ limit · downtrend+near-lows · earnings declining · margins contracting · negative news · changed-since-last-run) over a card → `tradeos briefing` prints a glanceable pre-market summary with only the flagged names. Descriptive flags, never advice. Live on the book: 5/5 flagged (INFY: top risk + downtrend + negative news; RELIANCE: earnings declining + margins contracting).
  - **+8 tests (93 → 101):** briefing rules (every rule fires / calm trips nothing / flagged-vs-quiet split) + the API via `TestClient` (health, holdings, JSON-safe narratives, 400 on bad as_of, 503 when DB-less). FastAPI/uvicorn added to deps.
  - **Phase 5 tail (named, not hidden):** a **scheduler** (cron `tradeos briefing` works today; APScheduler/Airflow later) and a **Telegram alert bot** — deferred because it needs a BotFather token only Shivansh can create; the alert *content* engine is done, so wiring Telegram is a thin delivery adapter once there's a token. Uncommitted (no-watermark convention).

- **Completed all open P1+ items — 6th analyzer in, calibrated confidence, what-changed delta, survivorship-free eval. 93 tests green, ruff + mypy clean.** After the P0 audit pass, finished every named-open deliverable. The discipline throughout: **build real where free+reliable+point-in-time data exists; build the seam and degrade to honest "no data" where it doesn't — never fabricate coverage** (Directive #8).
  - **All six analyzers now ship.** Added **Sentiment** (`sentiment.py`: transparent finance lexicon → `news_flow` dial from yfinance headlines) and **Ownership** (`ownership.py`: institutional/insider % from yfinance) agents → REGISTRY is now 6. Both are **descriptive-of-NOW snapshots with no usable history, so both are explicitly BARRED from the eval harness** (labelled in every output) — the honest call, since a non-point-in-time signal in a back-test is a look-ahead. Stored per-row (`sentiment`, `ownership` tables) so `analyze` stays offline/pure; ingest pulls them best-effort. Live read: TCS insider 71.8% (Tata Sons) ✓, INFY institutional 55% ✓.
  - **Macro FII/DII** — built the `MarketFlowSource` seam (sector exposure was already done). No free, reliable, point-in-time FII/DII feed exists, so the default returns None and macro prints "FII/DII flow: no source configured (deferred behind seam)" — a seam to fill, not a fake scrape.
  - **Survivorship-free eval universe (Directive #3, properly).** `eval` now runs on `load_universe()` = holdings ∪ `universe.csv` (sold/delisted names you declare), not just current holdings. Ingest pulls the universe too. The survivorship caveat is now conditional/honest ("reduced to whatever names you omit" vs "survivorship-biased; add names to fix"). The real fix (a point-in-time index-constituents feed) is named as the next data lift. → [[Earn the infra — adopt a tool only when you feel the problem it solves]]
  - **Calibrated confidence (`scoring.compute_confidence`).** A 0-1 confidence in the *read*, from **data completeness + input depth + signal coherence** (directional agreement across dials), mapped to high/medium/low with reasons. Deliberately **NOT a probability of return** — the note says so — because the signals aren't edge-validated; it's confidence the *description* rests on complete, coherent data. INFY reads "high (0.70) — 6/6 dimensions; signals mixed".
  - **Run-over-run what-changed delta (`snapshots.py`).** Each `analyze` persists a compact per-stock fingerprint to `run_snapshots`, then diffs the fresh card against the prior run — dial flips + threshold-gated score/risk moves. Pure `compute_delta` is unit-tested; `--no-snapshot` opts out. Fired live on first run with the new dims: "Δ vs last: attention 72 → 60" (sentiment+ownership pulled INFY's attention down vs the prior snapshot). The roadmap's "tells you *what changed*", shipped.
  - **+15 tests (78 → 93):** lexicon scoring, ownership buckets, confidence (completeness/coherence/empty), delta (flips, thresholds, no-phantom-flips on first data), universe union/dedup, macro-flows seam. **Still open (named):** real FII/DII + delisted feeds (seams ready, data isn't free); eval-derived score weights; Phase 5 (FastAPI + briefing); Phase 6 infra. Uncommitted (no-watermark convention).

- **Re-onboarding audit → closed the point-in-time fundamental leak + an eval-honesty pass (P0).** A read-only status audit against the Prime Directives (verifying *code*, not docs) found the machinery at bar but two silent look-aheads and an over-claimed eval. Fixed all P0 — **78 tests green (was 72), ruff + mypy clean.** No commit (no-watermark convention; left to me).
  - **The leak (Prime Directive #2, the named one).** The Fundamental agent filtered quarters by `period_end <= as_of`, but a quarter is only *public* ~45d later. `ANNOUNCEMENT_LAG_DAYS=45` was defined and wired to **nothing**. On any `--as-of` replay it surfaced results up to 45d before they existed. **Root cause + lesson:** a config knob without a call-site is worse than no knob — it *reads* as solved while leaking in the code. Fix: `_availability_cutoff(as_of) = as_of − lag`, applied in both `_load` and `load_fundamentals` (filter `period_end <= as_of − lag`). Verified live: at `as_of=2025-12-01` INFY now shows its **Sep-30** quarter (announced ~Nov-14), not the later unannounced ones. Live (`as_of=None`) unchanged — yfinance only returns announced quarters. → [[Validate the data, not just that it runs]]
  - **Guidance had *no* point-in-time filter at all.** `load_all_guidance(symbols)` took no `as_of`, so a historical card could quote a *future* concall. Now `load_all_guidance(symbols, as_of)` filters on the document's public date — `COALESCE(doc_chunks.filing_date, period + lag)` — and **excludes** guidance whose availability is unknown (honest gap over a look-ahead, Directive #8). Threaded `as_of` through `AnalysisContext.build` → `compute_all_fundamental`.
  - **Eval honesty pass (the differentiator, hardened).** (1) **Fundamental signals now evaluable**, read at availability (period_end + lag) via a proper as-of join onto trading dates — `net_margin / earnings_yoy / revenue_yoy`. (2) **Costs reported** (Directive #4): every signal shows gross *and* net LS spread after `COST_BPS` (~4 legs/cycle round-trip). (3) **Survivorship flagged** (Directive #3): universe = current holdings only → labelled "upward-biased" in the result + printout; delisted-inclusive universe named as the next data lift. (4) **Multiple-testing** caveat printed (n_signals, no deflation).
  - **Caught my own confidently-wrong number — the lesson the whole project is about.** First fundamental run printed `net_margin |t|=2.38` on **296 "dates"** — but a margin *level* is near-constant between quarters, so those daily ICs are massively overlapping; the real independent sample is ~14 quarters. NW(lag=20) under-corrects when signal persistence ≫ horizon. Fix: thin fundamental ICs to non-overlapping horizon-spaced windows (`FUND_NW_LAG=1`) → now honestly reads **n=14, t=−2.01**, which the multiple-testing + underpowered + survivorship flags then puncture (7 trials ⇒ needs |t|>~2.7). The harness now does its job: surface the number, then refuse to trust it. → [[Validate the data, not just that it runs]]
  - **Also added** point-in-time invariant tests for the Fundamental (lag) and Macro (terminal-row-only) agents — the Definition-of-Done requirement that was missing for both. **Open (named):** sentiment/ownership analyzers; delisted-inclusive eval universe; calibrated confidence on the composite; what-changed delta.

- **Closed out Phase 2 & 3 — completed every remaining roadmap deliverable, properly.** After an honest audit ("are 2 & 3 *actually* done?") found the functional milestones met but specific deliverables open, shipped all five, each tested + honest about the no-API-key constraint (LLM paths build + degrade gracefully; deterministic paths fully unit-tested). **72 tests green, ruff + mypy clean** (was 54).
  - **Phase 2 — synthesized score (`scoring.py`).** The roadmap card is "dials + *score* + trace"; the score was missing. Built a **descriptive attention score** (0-100): how much a holding *warrants a look* (notable risk/technical/fundamental/sector states) — explicitly **NOT** buy/sell. Key honesty call: Phase-4 just showed these signals have no significant edge, so a confident "rating" would be the over-confident story we warn against. So it's an attention *router*, decomposable (per-dimension sub-scores + drivers exposed) with **equal weights flagged as not-yet-edge-validated** and a hook to plug eval-derived weights in with zero code change. INFY reads 72/100 — top risk contributor + near lows + downtrend + 47% Tech book. → [[Validate the data, not just that it runs]]
  - **Phase 2 — Macro agent (`macro.py`).** The fourth analyzer: **sector exposure + concentration** (HHI / effective sectors) from per-holding sectors (yfinance `.info` → new `security_meta` table). The example book is **47% Technology → concentration "high"**, which now also feeds the attention score. FII/DII flow (the roadmap's other macro half) **deliberately deferred** — it needs a market-wide data feed; a fragile scrape isn't worth it for a personal book (same judgement as document-fetch).
  - **Phase 2 — LLM observability (`trace.py`).** Build-your-own (roadmap rule #5: understand it before adopting Langfuse): every Claude call records exact tokens + latency and a best-effort cost (configurable list-price table, flagged approximate); `analyze`/`risk` print a per-run `· LLM: N calls · tokens · ms · est cost` line. Langfuse-exporter-ready.
  - **Phase 3 — guidance extraction → Fundamental agent (`extraction.py`).** The real unbuilt half of Phase 3: RAG-retrieve a concall's guidance-relevant chunks → schema-constrained LLM extract (revenue/margin outlook, demand, **with verbatim cited quotes**) → store in a `guidance` table → the Fundamental agent now serves management's forward commentary alongside the yfinance ratios. `tradeos extract SYMBOL`. The Fundamental agent stops being numbers-only.
  - **Phase 3 — RAG eval harness (`rag_eval.py`, `tradeos rag-eval`).** "Eval before trust" applied to RAG, separating the two failure modes: **retrieval** (offline, no key — recall@k / answerable / best-distance vs a golden set) and **generation** (key-gated — citation grounding + answer-hit). On the synthetic INFY concall: 5/5 answerable, best-distances 0.13–0.34. Honest caveat printed: tiny corpus ⇒ illustrative; recall@k earns its meaning on real multi-doc filings.
  - **Still open (named, not hidden):** signal-eval transaction-costs/turnover + announcement-lagged *fundamental* signals; sentiment/ownership analyzers; Langfuse adoption. Next phase: 5 (FastAPI + frontend + daily briefing). Code remains uncommitted on `main` (left to Shivansh; no-watermark convention).

- **Document coverage / freshness check — made RAG incompleteness *visible* (`tradeos docs status`).** Prompted by the right question — *"is manual report ingestion actually reliable?"* The senior answer: it's not unreliable because it's *manual*, it's unreliable because **nothing in the system knew when it was incomplete** — a forgotten transcript meant `ask()` would confidently answer from a *stale quarter* with no warning (a silent wrong-data bug, the worst kind in a system you bet money on), and the doc layer had **no point-in-time provenance**, quietly undermining the Phase-4 honesty work. Fix shipped (reliability first, fetching later):
  - **Provenance on `doc_chunks`** — added `period` (quarter the doc covers), `filing_date` (point-in-time public date), `source_url`; idempotent `ADD COLUMN IF NOT EXISTS` migration (applied live + in `01_init.sql`). `add_document` + `tradeos docs add` now take `--period / --filing-date / --url`.
  - **`tradeos docs status`** — the **completeness signal already existed**: the `fundamentals` table knows each holding's latest reported quarter (`period_end`). Cross-reference that against the latest ingested doc `period` → per-holding flag: **MISSING / STALE / UNTAGGED / UNCHECKED / OK**. The classification is a pure, unit-tested `_coverage_flag()`. On the book today: 4 names MISSING, INFY UNTAGGED — gaps you can now *see* instead of discover the hard way. Verified the full lifecycle on the synthetic INFY doc: tagging it 2025-12-31 ⇒ **STALE** (results are 2026-03-31), re-tagging 2026-03-31 ⇒ **OK**. **54 tests, ruff + mypy clean.** → [[Validate the data, not just that it runs]]
  - **Deliberately did NOT build auto-fetch yet** — the coverage check is the *reliability* layer; a scraper is mere *convenience* and fails just as silently (NSE anti-bot, HTML drift). Plan when we do: a `DocumentSource` adapter mirroring `PriceSource`, screener.in-primary → BSE-announcements fallback → manual `docs add` as the always-works floor. ~60 docs/yr ⇒ best-effort, not a bulletproof pipeline (don't cargo-cult infra the problem doesn't need).

- **Pro-level senior-quant hardening pass — Phase 4 (eval) + Phase 3 (RAG).** Took the draft Phase 3/4 code from "works" to "a desk would sign off." All green: **52 tests** (was 44), `ruff` ✓, `mypy` ✓, CLI smoke-tested end-to-end. No new deps (Newey-West hand-rolled; no scipy).
- **Phase 4 — the eval harness now reports *uncertainty*, which is the whole point.** The old harness printed a single **pooled** Spearman as "IC" with **no standard error** — exactly the backtest-that-lies-to-you the roadmap warns against. Rebuilt it around the metrics a quant desk actually uses:
  1. **Cross-sectional rank IC** — per *date*, Spearman across names (signal-rank vs forward-return-rank), averaged over time. That isolates *"does the signal rank the cross-section correctly"* — the question a stock-selection signal must answer — instead of conflating it with time-series drift.
  2. **ICIR** = mean(IC)/std(IC) (descriptive), and the honest one: a **Newey-West (Bartlett-HAC) t-stat** with lag = horizon−1. Daily ICs built from *overlapping* h-day forward windows are autocorrelated; a naive SE overstates significance, so NW widens it. **|t|≳2 ≈ real.** Unit test locks the property (positively-autocorrelated series ⇒ |t| shrinks vs the iid SE).
  3. **Hit-rate vs a 50% null** (median signal → median forward return, which strips out the market-drift bias an absolute `fwd>0` test smuggles in) + the period **base rate** P(fwd>0) printed alongside. Per-date **long-short tercile spread** (gross of cost).
  4. **Pooled IC demoted to a labelled diagnostic** — it mixes time + cross-section and inflates `n`, so it's shown but never the headline.
  - **The teaching payoff, live on the 5-name book:** `trend_vs_200sma` shows **pooled IC +0.31** ("strong signal!") but **cross-sectional IC 0.0, t=0.0**. The harness now *exposes* precisely the inflated number the old metric reported as edge. Across all four signals **every |t| < 1 → no significant edge**, `rsi_14` IC negative as theory predicts for mean-reversion. Correct, honest, underpowered — and it *says so*. → [[Validate the data, not just that it runs]]
  - **Caveats baked into the output, not buried:** small universe ⇒ illustrative not conclusive (prints `n_dates` + the t-stat); **gross of transaction costs / turnover** (tercile spread is an upper bound — net-of-cost is the TODO); price signals only — fundamental-signal eval still needs the ~45d announcement lag (`ANNOUNCEMENT_LAG_DAYS`).
- **Phase 3 — RAG retrieval rigour.** Three real fixes (and one *non*-fix I refused to fake):
  - **Word-boundary chunking** — the char-window chunker split tokens mid-string (a figure like `23.5%` straddling two chunks is unretrievable). Windows now snap to whitespace on both ends, with a hard-cut fallback for pathological >window tokens. Test proves no token is ever fragmented.
  - **Relevance floor** (`RAG_MAX_DISTANCE`, default **0.45**, calibrated on the real concall: on-topic best hit ~0.30, off-topic ~0.47) — `ask()` flags a **low-confidence** answer when even the closest excerpt is too far, instead of confidently answering over chunks that don't cover the question. It never *hides* evidence, only cautions. Verified: off-topic query now prints the ⚠; on-topic doesn't.
  - **Structured + validated citations** — `ask()` now uses `messages.parse` + a Pydantic `CitedAnswer` (consistent with the risk/orchestrator agents) and **drops any cited excerpt number the model invented**, so a citation always points at a real retrieved chunk. Embedding-dimension safety assert added (catches a silent model/column mismatch).
  - **Refused to fake the "asymmetric embeddings" win:** verified empirically that for `bge-small-en-v1.5` in fastembed 0.8.0, `query_embed == embed` (cos 1.0) — so I wired `query_embed` at the query call-site *as the correct seam for a future model swap* but did **not** claim a retrieval gain that doesn't exist here. (A reranker / section-aware chunking on real 40-page concalls is still the next real lever.)
- **Fixed contradictory documentation (a hardening pass shouldn't leave false docs).** `risk.py`'s module docstring still claimed covariance was "unshrunk" and FHS was "a Phase-4 item" — **both are implemented and on by default**. Corrected the docstring and the matching stale lines in [[Risk engine methodology — TradeOS]]; refreshed the eval line in [[Architecture — TradeOS]].
- **Still uncommitted** — all of Phase 2/3/4 (incl. this pass) sits in the working tree on `main`; left committing to Shivansh (no AI watermark per repo convention). Honest gap-to-production unchanged: net-of-cost eval + broader universe, RAG reranker/semantic chunking, LLM tracing/observability, connection pooling.

## 2026-05-30

- **Pro-level refactor & hardening pass — agent framework + shared context + adapters.** Turned the ad-hoc analyzer modules into a real **agent framework**: a uniform `Agent` interface (`name` / `scope` / `run(ctx)`) + a `REGISTRY` the orchestrator iterates — adding macro/sentiment later is just "write the compute, wrap it, append." Introduced **`AnalysisContext`** that loads the price panels + all fundamentals **once** and hands them to every agent (before, each agent re-queried the DB and the Fundamental agent opened a connection *per holding* → now a single bulk query). Isolated market data behind a **`PriceSource` adapter** (yfinance today; bhavcopy/paid = one new class). Added **central logging** and stopped silently swallowing agent/synthesis/ingest errors. Fully backward-compatible: `analyze` numbers and the independent cross-validation are **identical** after the refactor; **44 tests** (added agent-framework + an injected-vs-loaded consistency test that locks the refactor), ruff clean.
  - **Honest gap-to-production (NOT yet done):** type-checking (mypy/pyright) + CI; connection pooling; the methodology upgrades flagged earlier (Filtered Historical Simulation VaR, Ledoit-Wolf covariance shrinkage, RAG semantic chunking + reranking, fundamental-signal eval with the ~45d announcement lag); broader eval universe; LLM tracing/observability. The *architecture & hardening* are pro-level; these are what would take it to full production grade.

- **Phase 4: eval harness — shipped, and it told the honest truth.** Point-in-time signal back-test: for each (stock, date) compute a signal from data ≤ that date, measure the realised 21-day **forward** return; pooled across holdings, report **Information Coefficient** (Spearman), hit-rate, and top-minus-bottom tercile spread. Subsamples dates to limit forward-window overlap and prints `n` so statistical power is visible. New `tradeos eval` command. **Result on the 5-name universe: every signal is noise** — momentum_3m IC −0.06, trend −0.06, RSI −0.05, low-vol +0.05 (all in the noise band), hit-rates ~50%, no real spread. *Exactly* what theory predicts for a tiny universe — and exactly the "validate before you trust it" thesis this whole project rests on. Caveat printed + logged: **illustrative, not conclusive** — real edge needs a broad cross-section (Nifty-50+) and longer history. **41 tests, ruff clean.**
  - Price-based signals only (no announcement lag); fundamental-signal eval must apply the ~30–45d results lag. → [[Validate the data, not just that it runs]]

- **Phase 3 (b): RAG "ask-the-call" — shipped (real pgvector).** Built **pgvector 0.8.2 from source against postgresql@16** (chose source build with `PG_CONFIG` over `brew install` because four PG versions are installed — deterministic targeting). Then the document-intelligence pipeline: parse (pymupdf for PDF; txt/md too) → overlapping chunks → **local fastembed embeddings** (BAAI/bge-small, 384-d, no API key) → `doc_chunks` table with an **HNSW cosine index** → vector search → cited answer via Claude. New commands `tradeos docs add <SYMBOL> <file>` and `tradeos ask <SYMBOL> "…"`. Retrieval fully offline; only the synthesised answer needs a key (without one, `ask` returns ranked excerpts). Verified on a synthetic concall — "what did management say about margins?" correctly retrieved the margins chunk by cosine distance. **37 tests, ruff clean.** Docs live under gitignored `data/`.
  - **Flagged for the pro-level hardening pass:** chunking is coarse char-windows — section/semantic-aware + smaller chunks (and a reranker) will sharpen retrieval on real 40-page concalls.

- **Phase 3 (a): structured Fundamental agent — shipped.** Probed yfinance first (the responsible move): RELIANCE returns ₹-crore quarterlies, **INFY returns USD** (its ADR) — so absolute revenue isn't cross-comparable. Built the agent to compute **ratios only** (currency-invariant): revenue/earnings YoY & QoQ growth, net/operating margins, margin trend → descriptive dials (growth strong/growing/flat/declining; margin expanding/stable/contracting). **Calendar-quarter YoY matching** (robust to yfinance's quarter gaps). Quarterly data ingested into a new `fundamentals` table so `analyze` stays offline/fast. Skipped EPS (yfinance returns 0) and `earnings_dates` (needs lxml) — Net Income growth is the reliable earnings signal. The orchestrator now runs **three agents** (risk + technical + fundamental) per card. Sanity-checked vs reality: INFY net margin ~18%, TCS ~19% — correct. **33 tests, ruff clean.**
  - **Caveat for Phase-4 eval:** fundamentals are filtered by `period_end` but were only *known* ~30–45d later (results-announcement lag) — a look-ahead to handle when back-testing fundamental signals.
  - **Next: Phase 3 (b)** — RAG "ask-the-call" over concall/results PDFs the user supplies.

- **Unified `tradeos` CLI + portfolio is now user-managed (demo data removed).** New `tradeos add / remove / holdings` commands (alongside `ingest / check / risk / analyze`) — type your stocks in the terminal and `add` **auto-fetches that symbol's data**, or edit `holdings.csv` directly (same source of truth, both stay in sync). **Wiped the hardcoded demo holdings**: `holdings.csv` is now empty + gitignored (real portfolio stays private), `holdings.example.csv` is the template. Ingestion is incremental (`ingest_symbols` fetches just what you add). **29 tests** (added portfolio add/remove + a conftest so integration tests run against the example portfolio, not the user's file).
  - **Clarified a design point:** the portfolio was *never* meant to be hardcoded — `holdings.csv` is the user input; the example rows were only build-time seed. Data source stays **yfinance** for now (right tool for daily EOD on a few names) but is isolated in one `fetch_ohlcv` adapter, so swapping to NSE bhavcopy / a paid feed later is a one-file change. → [[Architecture — TradeOS]]

- **Senior-quant model-validation pass + 2 fixes.** Independently recomputed every risk number from raw DB data with *different* methods (full-sample OLS, manual VaR, manual SMA) and diffed vs the engine. VaR/CVaR matched to the basis point, SMA/last-price exact, risk contribution structurally identical, vol coherent (EWMA > realized = current regime). Two genuine issues found and fixed:
  1. **Betas were noise.** They'd inherited the short EWMA window (~32 effective days) meant for volatility → jittery sensitivities (HDFCBANK 1.52, INFY 0.42, book 0.75). Beta is a *structural* exposure, so it now uses the **full sample** with **Bloomberg-style shrinkage toward 1** (β_adj = ⅔·raw + ⅓). Result: stable, believable betas (HDFCBANK 1.01, INFY 0.88, **book 0.89**) that cross-check exactly. Vol stays EWMA — you *want* current-regime vol. → [[Risk engine methodology — TradeOS]]
  2. **Technicals & risk shared one price series but want different adjustments.** Now store BOTH: `close` (split-adjusted price → SMA/RSI/52-wk levels, value, liquidity) and `adj_close` (total-return → returns/vol/VaR/beta) via yfinance `auto_adjust=False`. SMA200 correctly shifted to the price-chart level (1430 → 1436 — the dividend drift we'd been mixing in).
  - **Back-tested (2nd pass):** out-of-sample rolling-250d VaR exceedances 95% = 6.6% / 99% = 1.7% (vs 5% / 1% expected) — within ~1σ, mild conservative lean as expected for historical VaR on fat tails (→ why we also report CVaR). EWMA covariance is positive-definite, condition number 13.6 (well-conditioned at 5 names). No new bugs.
  - **Documented as intentional (not bugs):** vol is *conditional* (EWMA) while historical VaR is *unconditional* (full-2y empirical tail) — complementary; Filtered Historical Simulation is the consistent Phase-4 upgrade. Covariance is unshrunk (Ledoit-Wolf overkill at 5 names). **27 tests** (added a beta-shrinkage unit test), ruff clean. Lesson: passing tests ≠ a desk would sign off — cross-validate the numbers and interrogate which estimator each parameter deserves. → [[Validate the data, not just that it runs]]

- **Shipped: Phase 2 — multi-agent core (orchestrator + Technical agent).** A **hand-built orchestrator** (no framework yet — build-your-own-graph first) runs the portfolio Risk agent + a new per-stock **Technical agent** (SMA 20/50/200, RSI(14) Wilder, MACD, 52-wk position, 1m/3m returns, volume trend → descriptive dials: trend / momentum / level), merges each stock's technical read with its **risk slice** into a per-stock **card ranked by risk contribution**, and synthesises a reasoning trace per card via Claude **in parallel** (`ThreadPoolExecutor`) — `messages.parse` + Pydantic `StockCard`, descriptive-only. Factual cards work with no API key; synthesis is added when one is set. New command `tradeos-analyze` (`--horizon`, `--as-of`, `--no-llm`). **26 tests green** (added indicator + orchestrator tests), ruff clean.
  - **Pattern locked:** every analyzer agent = *pure-Python facts + optional LLM narration*; the orchestrator composes them. This is the template for the earnings / macro / sentiment agents next.
  - **Read today:** all 5 holdings print downtrend / weak momentum (RSI 35–44), most near 52-wk lows — coherent with the adjusted-price data. INFY is both the **top risk contributor (46.6%)** *and* technically weak — exactly the two-dimensional flag the orchestrator exists to surface.

- **Connected the project to the vault** — created this TradeOS space (hub + log + Tech/Sources notes + atomic beliefs) and a repo↔vault documentation protocol in `repo/CLAUDE.md` so future sessions keep this log updated. Mirrors the Arth Saathi convention.

- **Shipped: horizon generalisation (`--horizon`)** — vol and VaR/CVaR can now be expressed over any horizon (d/w/m/q/y or `Nd`). → [[Estimate once, express at any horizon]]
  - **Insight:** annualising (×√252) is just a *unit*, not a calculation — the underlying risk is horizon-agnostic. So we estimate once (daily) and scale by **√time** (σ_T = σ_daily·√T).
  - **What scales vs what's invariant:** vol/VaR/CVaR scale by √horizon; **correlation, beta, and risk-contribution % are ratios → identical at every horizon.** Verified: book vol reads 1.09% daily / 2.44% weekly / 5.0% monthly / 17.34% annual, while beta 0.75, avg corr 0.31, effective holdings 4.58 don't move.
  - **Limits stay pinned to natural units** (annual vol, 1-day VaR) regardless of the display horizon, so the risk budget keeps its meaning. Caveat documented: √time assumes i.i.d. returns — fine short-term, weakens over long horizons.

- **Audited the whole engine** — `ruff` clean, **18 pytest tests** (math validated against independent calcs: EWMA cov reduces to numpy population cov at λ=1; CVaR ≥ VaR; risk contributions sum to 100%; no look-ahead), data integrity (0 nulls/non-positive, max daily move ≤10% confirms adjusted prices clean), edge cases (single holding, point-in-time). **0 correctness bugs, 0 data issues.** Test suite now guards every future change.

- **Rebuilt Risk Agent → v2 quant engine** (thinking like a buy-side desk). → [[Risk engine methodology — TradeOS]], [[Weight is not risk — manage the risk contributor]]
  - **Headline reframe:** v1 showed weights + standalone vol; v2 leads with **component risk contribution** — INFY is 31.7% of capital but **46.6% of risk**. You manage the risk *contributor*, not the biggest position.
  - Added: **EWMA(λ=0.94) covariance** (regime-aware, replaces flat stdev), **MCTR/%CTR** (Euler decomposition, sums to 100%), **historical VaR & CVaR** (95/99, fat-tail-honest), **correlation matrix + avg pairwise**, **days-to-liquidate**, **stress** (worst 1/5/10/21-day windows), and **risk-limit breach flags**.
  - LLM layer upgraded to a buy-side risk-manager persona; still **descriptive-only** (no buy/sell), still never computes a number.

- **Caught + fixed a data bug via the rebuild** — ingestion used raw (`auto_adjust=False`) prices, so splits/bonuses showed up as fake −45/−50% drawdowns and poisoned every vol/drawdown number. Switched to **total-return adjusted** (`auto_adjust=True`) and re-ingested. Worst-21-day went from a bogus −50% to a realistic −11.7%. → [[Validate the data, not just that it runs]]

- **Shipped: Phase 1 — Risk Agent v1** — pure-Python risk (concentration, vol, beta, drawdown) + a thin Claude narration layer (`messages.parse` + Pydantic `RiskReport`, descriptive). Evolved `holdings.txt` → `holdings.csv` (symbol, quantity, avg_cost) and added a NIFTY benchmark (`^NSEI`) for beta.

- **Shipped: Phase 0 — Data foundation** — Python/`uv` project, `yfinance` ingestion → idempotent UPSERT, a `tradeos-check` verifier, 2 years of daily prices for the book + benchmark. **2,985 rows.**
  - **Pivoted the data layer** — planned Docker + TimescaleDB, but Docker Desktop was down and a **native Homebrew Postgres already held port 5432**. Used that instead (dedicated `tradeos` DB, trust auth). Stock Postgres has no TimescaleDB extension → hypertables deferred to Phase 6. → [[Earn the infra — adopt a tool only when you feel the problem it solves]]

- **Wrote the learning-first roadmap** (`repo/ROADMAP.md`) — each phase ships something usable *and* teaches a skill; frontend deferred to Phase 5, heavy infra to Phase 6 (earn it, don't cargo-cult).

- **Reframed the whole idea: product → personal learning project.** Killed the SaaS framing after an honest review — SEBI regulates stock-specific advice, the combined signals likely lack real alpha, and the market is saturated. As a personal tool + portfolio piece the constraints lift and the project gets *better*. Decided to build it on my own holdings.

## Template entry
```
## YYYY-MM-DD
- What I shipped:
- What I decided:
- What's blocked:
- Next:
```

## Related
- [[TradeOS]]
